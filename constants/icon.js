import {
  faCircleNotch as falCircleNotch,
  faPlus as falPlus,
  faThList as falThList,
  faTimes as falTimes,
  faTrash as falTrash,
} from "@fortawesome/free-solid-svg-icons";

import { faGithub as fabGithub } from "@fortawesome/free-brands-svg-icons";
export const addIcon = falPlus;
export const deleteIconLight = falTimes;
export const emptyIconLight = falCircleNotch;
export const loadingIconLight = falCircleNotch;
export const listIcon = falThList;
export const trashIcon = falTrash;
export const gitHubIcon = fabGithub;
